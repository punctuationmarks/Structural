{
  "name": "structural-interview",
  "version": "0.1.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon server.js",
    "tsc": "tsc *.ts --resolveJsonModule",
    "test": "mocha tests/*.serverSpec.js",
    "tsc-test": "tsc *.ts --resolveJsonModule && tsc tests/*.ts --resolveJsonModule && mocha tests/*.serverSpec.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.4.0",
    "nodemon": "^2.0.6"
  },
  "devDependencies": {
    "@types/mocha": "^8.0.4",
    "@types/node": "^14.14.10",
    "babel-register": "^6.26.0",
    "chai": "^4.2.0",
    "mocha": "^8.2.1"
  }
}
